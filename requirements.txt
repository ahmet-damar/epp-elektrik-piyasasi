streamlit>=1.40
# altair: Streamlit'in KENDİ bağımlılığı olarak zaten kuruluydu (YENİ bir
# bağımlılık EKLENMEDİ) — app/dashboard.py artık doğrudan import ettiği
# için (Zaman Serisi bölümü, 2026-09-20) burada da AÇIKÇA pinlendi, aksi
# hâlde streamlit ileride kendi bağımlılığını değiştirirse sessizce kırılır.
altair>=5,<7
pandas>=2.0
numpy>=1.26
openpyxl>=3.1
requests>=2.31
python-dotenv>=1.0
psycopg[binary]>=3.2
supabase>=2.9.0
python-docx>=1.1
